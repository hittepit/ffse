package be.hittepit.ffse.model

import org.scalatest.FunSuite
import org.scalatest.matchers.MustMatchers

class TestState extends FunSuite with MustMatchers{
//	test("adding a new event create a new state with transition added"){
//	  val s = new State("test",Map.empty)
//	  val e = new Event("test")
//	  val s1 = s.addEvent(e,s)
//	  s1.events.size must be(1)
//	}
//	
//	test("adding an event that is already defined in state throws an exception"){
//	  val e = Event("go")
//	  val s = new State("test",Map(e->new State("out",Map.empty)))
//	  evaluating{s.addEvent(e,s)} must produce [Exception]
//	}
}